# Debezium Server: WAL таблицы chunks -> Kafka -> src/kafka_service/cdc.py -> Qdrant.
# Standalone-режим вместо Kafka Connect: источник один, кластер коннекторов не нужен.
# Монтируется в контейнер как /debezium/conf/application.properties.

debezium.sink.type=kafka
debezium.sink.kafka.producer.bootstrap.servers=kafka:29092
debezium.sink.kafka.producer.key.serializer=org.apache.kafka.common.serialization.StringSerializer
debezium.sink.kafka.producer.value.serializer=org.apache.kafka.common.serialization.StringSerializer
debezium.sink.kafka.producer.acks=all

debezium.source.connector.class=io.debezium.connector.postgresql.PostgresConnector
debezium.source.database.hostname=postgres
debezium.source.database.port=5432
debezium.source.database.user=babufrik
debezium.source.database.password=password
debezium.source.database.dbname=babufrik

# Топик собирается как <topic.prefix>.<schema>.<table> -> cater.public.chunks.
debezium.source.topic.prefix=cater

# pgoutput встроен в Postgres 10+, отдельных расширений ставить не нужно.
debezium.source.plugin.name=pgoutput
debezium.source.slot.name=cater_qdrant_sync
debezium.source.publication.name=cater_qdrant_pub
# filtered: публикация только на нужные таблицы, а не на всю базу.
debezium.source.publication.autocreate.mode=filtered

debezium.source.table.include.list=public.chunks
# chunks.text — тело чанка; в WAL-события оно не нужно и раздуло бы топик.
debezium.source.column.exclude.list=public.chunks.text

# no_data: снимок схемы без вычитывания существующих строк. Исторические сироты
# убираются один раз командой `python -m src.kafka_service sweep`, а не через
# перелив всей таблицы chunks в Kafka.
debezium.source.snapshot.mode=no_data

# Tombstone после delete — нужен для compaction; консьюмер их пропускает.
debezium.source.tombstones.on.delete=true

# КРИТИЧНО для дискового здоровья: если в chunks тихо, а база пишет в другие
# таблицы, LSN слота не двигается и WAL копится до заполнения диска.
# Heartbeat регулярно подтверждает позицию слота.
debezium.source.heartbeat.interval.ms=30000

debezium.source.offset.storage.file.filename=/debezium/data/offsets.dat
debezium.source.offset.flush.interval.ms=10000

# Плоский JSON-конверт {before, after, op, source} без Connect-схем внутри сообщения.
debezium.format.key=json
debezium.format.key.schemas.enable=false
debezium.format.value=json
debezium.format.value.schemas.enable=false

quarkus.log.console.json=false
quarkus.log.level=INFO
